# Personal data - each user keeps locally, never committed
# Exclude all user data EXCEPT the _template folder
users/*/bullet-corpus.txt
users/*/current-resume.*
users/*/profile.md
users/*/job-applications/

# But keep everything in _template
!users/_template/
!users/_template/**

# User resume docs
users/*/*.docx

# System files
.DS_Store
__pycache__/
*.pyc

# Scratch/temp files during processing
scratchpad/
